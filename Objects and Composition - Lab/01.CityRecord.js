function cityRecord(name,population,treasury) {
    'use strict';
    
    const obj = {
        name: name,
        population: population,
        treasury: treasury
    };

    return obj;
}
cityRecord("Tortuga", 7000, 15000);
